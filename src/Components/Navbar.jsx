// Exporting the NavBar component as the default export
export default function NavBar() {
  // The component returns a navigation bar element
  return (
    // Inline styles
    <nav style={{ backgroundColor: "#0d6efd", padding: "1rem", color: "#fff" }}>
      <h1>Southern Alberta Institute of Technology</h1>
    </nav>
  );
}
